rules:
  - id: azurpilot-shell-invocation
    languages:
      - python
    message: "Не используйте shell invocation в ограниченном subprocess прямой интеграции."
    severity: WARNING
    patterns:
      - pattern-either:
          - pattern: os.system(...)
          - pattern: os.popen(...)
          - pattern: subprocess.$FUNCTION(..., shell=$FLAG, ...)
          - pattern: run(..., shell=True, ...)
          - pattern: call(..., shell=True, ...)
          - pattern: check_call(..., shell=True, ...)
          - pattern: check_output(..., shell=True, ...)
          - pattern: Popen(..., shell=True, ...)
      - pattern-not: subprocess.$FUNCTION(..., shell=False, ...)
